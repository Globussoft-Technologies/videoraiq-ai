import CameraGrid from '../../../components/CameraGrid';

/** Playback — single-screen recorded-footage viewer with a scrubbable timeline, independent from Live Wall. */
export default function CameraView() {
  return <CameraGrid />;
}
