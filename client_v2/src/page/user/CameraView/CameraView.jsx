import CameraGrid from '../../../components/CameraGrid';

/** Camera View — live multi-camera grid (HLS), default single-up focus. */
export default function CameraView() {
  return <CameraGrid defaultCols={2} />;
}
